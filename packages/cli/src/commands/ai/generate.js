import { Agent, Bash } from \"@claude-code/tool-sdk\";\n\nexport default async function aiGenerateCmd(instruction) {\n  const aiResponse = await Agent({\n    description: \"Parse user instruction and generate appropriate CLI command for module/page generation.\",\n    prompt: `The user wants to generate code using existing CLI commands (e.g., 'generate module', 'generate page'). Your task is to parse the following instruction and output the exact CLI command to be executed. Ensure all necessary arguments are included and naming conventions are preserved.\\n\\nUser Instruction: ${instruction}\\n\\nExample (user wants a module):\\nInstruction: \"create a user module with fields name:string and email:email:required\"\\nOutput: \"generate module user --fields name:string;email:email:required\"\\n\\nExample (user wants a page):\\nInstruction: \"create a login page with a form\"\\nOutput: \"generate page login --withForm\"\\n\\nOutput only the command, without any extra text or explanations.\\n`,\n    subagent_type: \"general-purpose\",\n  });\n\n  const cliCommand = aiResponse.content.trim();\n  console.log(`Executing generated command: ${cliCommand}`);\n  await Bash({ command: cliCommand, description: `Execute generated CLI command: ${cliCommand}` });\n}\n