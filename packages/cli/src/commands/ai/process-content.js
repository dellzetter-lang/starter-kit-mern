import { Agent } from \"@claude-code/tool-sdk\";\n\nexport default async function aiProcessContentCmd(content, options) {\n  const aiResponse = await Agent({\n    description: \"Process user-provided content with AI for fixes and logic application.\",\n    prompt: `Analyze the following content and apply the requested fixes/logic while preserving existing naming conventions. Only return the modified content.\\n\\nUser Request: ${options.prompt}\\n\\nContent:\\n\`\`\`\\n${content}\\n\`\`\`\\n`,\n    subagent_type: \"general-purpose\",\n  });\n\n  console.log(aiResponse.content);\n}\n